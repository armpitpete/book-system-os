# Production hardening scope lock

The planning branch contains documentation only.

Runtime files remain unchanged. The authorised v0.1.8 deployment commit on `main` remains unchanged.

Issue #31 is the authority for H-01 through H-09. Any additional hardening gate requires a reviewed scope change and must not be inserted silently.
